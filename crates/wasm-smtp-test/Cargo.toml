[package]
name = "wasm-smtp-test"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Test transport and fixtures for wasm-smtp (dev-only, not for production use)."
readme = "README.md"
keywords = ["smtp", "email", "testing", "mock"]
categories = ["email", "development-tools::testing"]

[lib]
path = "src/lib.rs"

[dependencies]
wasm-smtp = { path = "../wasm-smtp", version = "0.17.1" }

[lints]
workspace = true
